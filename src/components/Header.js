import React from "react"
import NavBar from "./NavBar"
const Header = (props)=> {
    return (
        
            <NavBar estado={props.estado}/>
            
        
    )
}
export default Header

